name: OpenAI GPT-4 Turbo
version: 1.0.0
schema: v1

models:
  - name: OpenAI GPT-4 Turbo
    provider: openai
    model: gpt-4-turbo
    apiKey: ${{ inputs.OPENAI_API_KEY }}
    roles:
      - chat
      - edit
      - apply