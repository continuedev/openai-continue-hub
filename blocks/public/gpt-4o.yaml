name: OpenAI GPT-4o
version: 1.0.0
schema: v1

models:
  - name: OpenAI GPT-4o
    provider: openai
    model: gpt-4o
    apiKey: ${{ inputs.OPENAI_API_KEY }}
    roles:
      - chat
      - edit
      - apply